{
  "name": "boardwalk",
  "private": true,
  "description": "Static games monorepo deployed to boardwalk.tristanpendergrass.com via Netlify",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "serve dist",
    "deploy:draft": "netlify deploy",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^21.0.0",
    "serve": "^14.2.4"
  }
}
